import React, { useState } from 'react';

const RequestForm = () => {
  const [formData, setFormData] = useState({
    systemName: '',
    reqUserName: '',
    replyUserName: '',
    replyPassword: '',
    deliveryMode: '',
    paamIdentifierType: '',
  });

  const handleChange = (e) => {
    const { name, value } = e.target;
    setFormData((prevData) => ({
      ...prevData,
      [name]: value,
    }));
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    console.log('Form Submitted:', formData);
    alert('Form submitted successfully!');
    setFormData({
      systemName: '',
      reqUserName: '',
      replyUserName: '',
      replyPassword: '',
      deliveryMode: '',
      paamIdentifierType: '',
    });
  };

  return (
    <div>
      <h2>Request Form</h2>
      <form onSubmit={handleSubmit}>
        <label>System Name:</label>
        <input
          type="text"
          name="systemName"
          value={formData.systemName}
          onChange={handleChange}
          required
        />

        <label>Req_User_Name:</label>
        <input
          type="text"
          name="reqUserName"
          value={formData.reqUserName}
          onChange={handleChange}
          required
        />

        <label>Reply User Name:</label>
        <input
          type="text"
          name="replyUserName"
          value={formData.replyUserName}
          onChange={handleChange}
          required
        />

        <label>Reply Password:</label>
        <input
          type="password"
          name="replyPassword"
          value={formData.replyPassword}
          onChange={handleChange}
          required
        />

        <label>Delivery Mode:</label>
        <select
          name="deliveryMode"
          value={formData.deliveryMode}
          onChange={handleChange}
          required
        >
          <option value="">-- Select Delivery Mode --</option>
          <option value="SYNC">SYNC</option>
          <option value="ASYNC">ASYNC</option>
        </select>

        <label>PAAM Identifier Type (1-9):</label>
        <select
          name="paamIdentifierType"
          value={formData.paamIdentifierType}
          onChange={handleChange}
          required
        >
          <option value="">-- Select PAAM Identifier --</option>
          {Array.from({ length: 9 }, (_, i) => (
            <option key={i + 1} value={i + 1}>
              {i + 1}
            </option>
          ))}
        </select>

        <button type="submit">Submit</button>
      </form>
    </div>
  );
};

export default RequestForm;
